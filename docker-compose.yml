services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./talentmatch.db
  web:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
